name: vendorbot_env
channels:
  - conda-forge
  - defaults
dependencies:
  - pip
  - python=3.8.0
  - ipython
  - pip:
    - python-telegram-bot
    - pymongo
    - Faker
    - certifi
    - cffi
    - cryptography
    - decorator
    # - future
    - pip
    - pycparser
    - python-telegram-bot
    - setuptools
    - six
    - tornado
    - wheel
    # - pythonnet
    - dateparser
prefix: C:\anaconda3\envs\vendorbot_env

